#include <stdio.h>
int main()
{
  int n,S,max;
  printf("enter N number of coloms : ");
  scanf("%d",&n);
  int T[n];
  int ;
  printf("enter numbers you want : ");
  for(int i=0;i<n;i++){
    scanf("%d",&T[i]);
  }
max=T[0];
  for(int i=1;i<n;i++){
            if(max<T[i])
            max=T[i];

        }
        printf("THE  MAX IS %d",max);
    return 0;

    }